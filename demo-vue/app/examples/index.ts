import BaseDemo, { title as BaseDemoTitle } from './Base.vue';

export const getExamples = () => {
    return [
        {
            title: BaseDemoTitle,
            component: BaseDemo
        }
    ];
};
